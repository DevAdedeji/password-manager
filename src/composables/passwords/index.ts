export const usePassword = () => {
    const fetchPasswords = async () => {
        try {
            console.log("Hello")
        } catch (e) {
            console.log(e)
        }
    }
    return { fetchPasswords }
}